#pragma once

#include <poweComponent.h>

class PlayModeObserver;
class Observer;
class DynamicSceneData : public powe::Component<DynamicSceneData>
{
public:

	DynamicSceneData();

private:

	SharedPtr<PlayModeObserver> 
};

